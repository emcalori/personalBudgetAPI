{
    "type": "object",
    "required": ["name", "budget"],
    "allOf": [{
        "properties": {
            "name": {"type": "string"},
            "budget": {"type": "integer", "minimum": 0}
        },
        "additionalProperties": false
    }]

}