{
    "type": "object",
    "required": ["name", "id", "budget"],
    "allOf": [{
        "properties": {
            "name": {"type": "string"},
            "id": {"type": "integer", "minimum": 1},
            "budget": {"type": "integer", "minimum": 1}
        },
        "additionalProperties": false
    }]

}